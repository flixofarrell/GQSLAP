language: python

install:
  - pip install cgatcore
  - pip install pyyaml
  - pip install ruffus
  - pip install gevent
  - pip install paramiko
  - pip install pep8
  - pip install pytest
  - pip install pytest-pep8
  - pip install drmaa
  - pip install pyyaml
  - pip install setuptools
  - pip install six
  - pip install sqlalchemy

# command to run tests
script:
  - python BBslap.py make full --local 
